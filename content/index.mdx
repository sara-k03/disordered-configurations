import React from "react";
import { Box, Container, Center, Text, Button } from "@chakra-ui/react";
import { Searchbox } from "@/components/Searchbox";
import { RequireAuthentication } from "@/components/RequireAuthentication";

<Container maxW='container.lg'>
  <RequireAuthentication>
    <Searchbox />
  </RequireAuthentication>
</Container>

**Directions to use**
- As of right now, you will need to type the exact configuration name you are looking for.
This is usually PCxxxx or something similar. 
- To download the .zip file containing all the files associated with that configuration, 
copy and paste the URL listed under each configuration into your browser. This will be updated 
to be clickable soon. 